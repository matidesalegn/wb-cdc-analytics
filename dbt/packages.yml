# dbt_utils only. Every other package considered was solving a problem this project
# does not have, and each one is a version constraint to carry.
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.3.0", "<2.0.0"]
